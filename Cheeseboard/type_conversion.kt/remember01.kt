/**
 * Let's write a lie detector program!
 * The program should take a string as an input, convert it
 * to a boolean value, and return it as the result.
 */
fun main() {
    val isLie = readLine()!!
    println(isLie.toBoolean())
}