/**
 * Write a program that reads two strings and
 * outputs them in the reverse order, each in a new line.
 */
fun main() {
    // Read data
    val a = readLine()!!
    val b = readLine()!!

    // Print values
    println(b)
    println(a)
}